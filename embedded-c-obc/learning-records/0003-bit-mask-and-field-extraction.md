# Bit mask and field extraction

The user completed the bit-mask exercise and correctly used named masks, parenthesized bit checks, low-byte extraction, and right-shift-plus-mask field extraction. Future lessons can assume basic bit testing and field extraction are usable, while continuing to reinforce cast placement and precedence review.
