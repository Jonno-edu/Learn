# Signed sensor values

The user completed the signed sensor-value exercise and used the intended decode sequence: combine bytes into a `uint16_t` pattern, cast the complete pattern to `int16_t`, and scale with `int32_t` arithmetic. Future lessons can introduce packet parsing and bounds checks while assuming basic signed byte decoding is usable.
