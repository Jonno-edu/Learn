# Volatile hardware registers

The user completed the first C register exercise and explained that `volatile` is needed because hardware or a bus can change register values outside normal C assignments. Future lessons can build on this, while reinforcing the sharper distinction that `volatile` preserves individual hardware-visible accesses but does not provide synchronization or correctness by itself.
